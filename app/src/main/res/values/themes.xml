<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NorthThompsonPizzariaLandingActivity" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NorthThompsonPizzariaLandingActivity" parent="Base.Theme.NorthThompsonPizzariaLandingActivity" />

    <style name="CustomRatingBarStyle" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/custom_rating_bar</item>
        <item name="android:minHeight">16dp</item>
        <item name="android:maxHeight">32dp</item>

    </style>
</resources>